# Which assistant bundle a production build loads. This is the CDN the site has always
# used, so a normal build behaves exactly as it did before this file existed.
VITE_ASSISTANT_BUNDLE_URL=https://assistant-script.nobi.ai/nobi.bundle.js
