# The assistant bundle every page loads; index.html reads this at build time.
# Committed on purpose so production is unchanged by default. To point at a
# local bundle build, override in .env.local (gitignored).
VITE_ASSISTANT_BUNDLE_URL=https://assistant-script.nobi.ai/nobi.bundle.js
